function word_reverser(string) {
	return string.split('').reverse().join('');
}

//defined function. Gave string. Split string. Reversed string. Joined string.

// created string hello. set string equal to true.
var string = "hello";

var word = true;

if (word = true ) {
	console.log(word_reverser(string));
}

// if string equals true than the function happens. 
